---
type: plan
project: vault
slug: sandbox-probe
repos: [vault]
status: executed
process_record: 2026-09-21-1600-sandbox-probe.trail.md
arch_spec: 2026-09-21-1600-sandbox-probe.arch.md
human_plan: https://claude.ai/artifact/KbDZT9w59Jqwjd3mmj4gzq
session:
tags: [plan, probes, sandbox, v-cr]
---

# sandbox-probe — plan

## Task
Session S10 of `vault/plans/2026-09-21-0900-architecture-first-planning.md`. Run the probes that execute repo code, and the repo's own rule files, inside the `/v-cr --sandbox` container, and tag their rows for the review panel (contracts C-2, C-3, C-6, C-8). Keywords: `probe-sandbox`, `probe-image`, `PROBE_TOOLS_FROM`, `--changed-list`, `--rows-from`, `framework.tsv`, `rules.tsv`.

## Open & deferred
- needs the operator: approve the five decisions SP-1 to SP-5, and the two departures from the S8 design that SP-6 and SP-7 record (mount layout, framework copy).
- size: this plan touches 29 files, past the ~15 at which a session drops work. The count is 11 product files, 3 test files, 2 fixtures and 7 graders.
- size: the other 6 files are this plan, its spec and its record, the master plan row, one row of the v-rule plan and `checks/rule-SC-7.sh`.
- size: the graders and fixtures are written, and every product file except `bin/probe-sandbox.sh` is a small edit.
- accepted limit: a tool that a pull request file can steer (`typos` reads `_typos.toml`, `claude` reads `plugin.json`) may print rows under its own id. They read `[confirmed]`; SP-3 limits the damage to that id and its message text.
- accepted limit: graders SC-2 to SC-4 test the driver's decisions against a test double of docker. Only SC-5 tests the envelope, with an image that holds git.
- accepted limit: a review with `--sandbox` and no probe image runs the `pr` posture and reads `Probes: sandbox not started`, and a review reads INCOMPLETE until the image holds every tool (SP-1, SP-2).
- accepted limit: a SIGKILL of the driver leaves its containers and work directory until `/v-cr --sandbox-gc` runs; a TERM or INT stops the container and removes both.
- accepted limit: the driver measures the tree before it copies it, so a hostile tree is bounded by the file and byte limits and not by the copy.
- unverified: no real `lizard` or `typos` image exists on this machine. `checks/sandbox-probe-SC-5.sh` runs real docker with `vault-tests:local`, which has neither, so it proves the path and the `absent:` report, not a tool row.
- existing defect, not this plan's: `bin/rule-count.sh --assert` fails on `HEAD` (181 rule lines, budget 173). This plan adds none.
- existing defect, not this plan's: 5 unit tests fail on `HEAD` (`document-standard.bats` two cases, `plugin-install.bats` two path notes, `research-clarify.bats` the PROPOSE output contract).

## Open questions

| id | question | blocks | searched | status | answer |
|----|----------|--------|----------|--------|--------|
| Q-1 | Is `lib/probe-run.sh` edited? | no | `lib/probe-registry.sh` `probe_path`, `lib/probe-run.sh` `probe_exec` and `probe_tool_present` | defaulted | no; both callers read `probe_path`, so one edit covers them (SP-12) |
| Q-2 | Does the sandbox posture also run the host `pr` posture? | no | `commands/v-cr/steps/03-review.md`; `bin/probe-panel.sh` | defaulted | no; the posture reads the rows of the run only (SP-9) |

## Success criteria
<!-- Checks are written before the work and exit 2 with "not written yet" until the file they grade exists. -->

| id | criterion | kind | how | check | expect | verdict | evidence |
|----|-----------|------|-----|-------|--------|---------|----------|
| SC-1 | WHEN `bin/probe.sh diff` gets `--changed-list <file>` THE SYSTEM SHALL read the changed files from the list, run no git, drop an escaping, absolute or missing path, and exit 2 for an unreadable list, for `--base` beside it and for `run`; and WHEN `PROBE_TOOLS_FROM=image` is set THE SYSTEM SHALL keep the repo's tool directories out of PATH, and exit 2 for any other value | functional | command | `checks/sandbox-probe-SC-1.sh` | exit 0 | MET | `checks/sandbox-probe-SC-1.sh` exited 0 |
| SC-2 | WHEN `bin/probe-sandbox.sh run` executes THE SYSTEM SHALL start one framework container and one container per template row with `--only`, each with the S0 envelope (no network, read-only, no capabilities, memory, cpu and pid limits, tmpfs `/tmp`, label, three environment variables), mount a framework copy holding `bin`, `lib` and `probes` and no `vault/` or `.git`, mount a repo copy with no `.git`, scrub the streams, stop a hung container by label, cut an oversize stream, remove its work directory, and write the four files of C-8 | functional | command | `checks/sandbox-probe-SC-2.sh` | exit 0 | MET | `checks/sandbox-probe-SC-2.sh` exited 0 |
| SC-3 | WHEN a base row is hand-written, a container prints a row or a ran line for an id that did not run, a container prints no ran line, the pull request edits the rules, a base id names a framework probe, a rule file is a link, the base has no `probes/`, a container fails or times out, or the image, docker or the `probe-image` key is missing THE SYSTEM SHALL run no repo script and no probe on the host, drop and report each forged row, and report each other case in the status lines of VR-4 | functional | command | `checks/sandbox-probe-SC-3.sh` | exit 0 | MET | `checks/sandbox-probe-SC-3.sh` exited 0 |
| SC-4 | WHEN `bin/probe-panel.sh run --posture sandbox --rows-from <dir>` reads the four files THE SYSTEM SHALL print each row `[confirmed]` from the file that held it, run no probe on the host, keep row text from moving a row or closing the fence, read a skipped, failed or missing status as INCOMPLETE with the operator line, read `no-rules:` as complete, and exit 2 for a missing `--rows-from`, for it beside another posture and for a missing directory | functional | command | `checks/sandbox-probe-SC-4.sh` | exit 0 | MET | `checks/sandbox-probe-SC-4.sh` exited 0 |
| SC-5 | WHEN a review runs the probe stage with real docker and a real image THE SYSTEM SHALL write a framework row and a template-rule row, report the hand-written row skipped, leave no container or work directory, and let the panel print both rows `[confirmed]` | delivery | command | `checks/sandbox-probe-SC-5.sh` | exit 0 | MET | `checks/sandbox-probe-SC-5.sh` exited 0 |
| SC-6 | WHEN an operator opens `lib/cr-sandbox.sh`, `commands/v-cr/sandbox.md`, the two `/v-cr` steps, `commands/_shared/probe-kit.md`, `commands/_shared/critic-panel.md` and ADR-009 THE SYSTEM SHALL show the probe image as an envelope key no indication sets, the stage in one home, the no-fallback rule once, the addendum, and no more than 181 rule lines in the corpus | delivery | command | `checks/sandbox-probe-SC-6.sh` | exit 0 | MET | `checks/sandbox-probe-SC-6.sh` exited 0 |
| SC-7 | WHEN the unit suite runs THE SYSTEM SHALL pass `sandbox-probe.bats`, `cr-sandbox.bats`, `v-cr.bats`, `probe.bats`, `probe-panel.bats` and `rule.bats`, and the full suite SHALL fail only the 5 tests that fail on `HEAD` | delivery | command | `checks/sandbox-probe-SC-7.sh` | exit 0 | MET | `checks/sandbox-probe-SC-7.sh` exited 0 |

## Definition of done

| id | line | state | evidence |
|----|------|-------|----------|
| B1 | change does what the task asked | met | `bin/gate.sh verdict <plan> --run` reports SC-1 to SC-7 MET; scope cut: none |
| B2 | tests covering the change pass | met | `checks/sandbox-probe-SC-7.sh` exited 0: the full unit suite fails only the 5 tests that fail on `HEAD`; `tests/unit/sandbox-probe.bats` 10 of 10 |
| B3 | lint passes on changed files | met | `./bin/doc-lint.sh --changed` exits 0 |
| B4 | every review finding fixed or recorded | met | every confirmed major finding of both review rounds fixed; the rest recorded in the trail and Open & deferred |
| B5 | invalidated docs updated | met | master plan row S10 and contract C-8; ADR-009 addendum; `commands/v-cr/sandbox.md` S8; `commands/_shared/probe-kit.md` |
| B6 | nothing unrelated in the commit | met | `git status --short` lists only this plan's files; `output-styles/director.md` and `scripts/completion-hook.sh` stay unstaged |

## Enforcement states

| id | ruling | state | mechanism |
|----|--------|-------|-----------|
| E-1 | No repo-code probe runs on the host when the container cannot run; posture `pr` may run the native probes | ENFORCED | `bin/probe-sandbox.sh` has no host-run path and posture `sandbox` reads rows only; `checks/sandbox-probe-SC-3.sh`, `checks/sandbox-probe-SC-4.sh` |
| E-2 | Rules that judge a pull request come from the merge base | ENFORCED | the driver writes the mount from `git cat-file` blobs; `checks/sandbox-probe-SC-3.sh` |
| E-3 | An indication never sets the probe image | HALF-BUILT | `cr_probe_image` reads only `VCR_SANDBOX_MAP` and `checks/sandbox-probe-SC-6.sh` tests it; the strip of an indication key is a step of `commands/v-cr/sandbox.md` §S0, applied by the session |
| E-4 | Probes never install a tool | ENFORCED | the container has no network and the kit prints `absent:`; `checks/sandbox-probe-SC-5.sh` |

## Verified current state
- `bin/probe.sh diff` lists changed files through `probe_changed`, which runs git · `bin/probe.sh` `file_list`, `lib/probe-scope.sh` · 2026-09-21
- `probe_files` returns 2 when `.git` exists and git cannot read it, and its `find` mode skips `node_modules` and `vendor` · `lib/probe-scope.sh` · 2026-09-21
- `probe_path` appends the repo's tool directories for a `yes` row, and both `probe_exec` and `probe_tool_present` call it · `lib/probe-registry.sh`, `lib/probe-run.sh` · 2026-09-21
- `probe_check_rows <id>` rewrites field 1 to one id, so a stream of many ids needs one call per id · `lib/probe-run.sh` · 2026-09-21
- a template row is the output of `bin/rule-check.sh row --slug <s> --stack <x>` · `bin/rule-check.sh` · 2026-09-21
- `bin/probe-panel.sh` runs `bin/probe.sh diff` on the host, marks a repo-registry row advisory and reads `probe_changed` on the host to set `registry-edited` · `bin/probe-panel.sh` · 2026-09-21
- no code reads `VCR_SANDBOX_MAP`, and no script runs `docker run` for a review · `grep -rl 'VCR_SANDBOX_MAP\|docker run' bin lib` · 2026-09-21
- a rule line of `bin/rule-count.sh` is a line of the 12 corpus files that matches a prohibition or requirement word; the total is 181 · `bin/rule-count.sh` · 2026-09-21
- `tests/Dockerfile` builds `vault-tests:local` from alpine with bash, git, grep, findutils, coreutils, jq and python3; it holds no `lizard` or `typos` · `tests/Dockerfile` · 2026-09-21
- `docker` 29.3.1 is installed, `lizard` and `typos` are not on PATH, `jq` is · `command -v docker lizard typos jq` · 2026-09-21
- `cr_redact_runtime` is a text scrubber that collapses trailing newlines · `lib/cr-sandbox.sh` · 2026-09-21

## Decisions
<!-- Ids SP-n are local to this plan; VR-n names a decision of `vault/plans/2026-09-21-1430-v-rule.md`. -->

| decision | reason | record |
|----------|--------|--------|
| SP-1 The probe image is named by the key `probe-image` of the user or global `VCR_SANDBOX_MAP`, built by the operator, and never built or pulled by the framework (`docker image inspect` first, then `--pull never`). It needs bash 4.4 or later, awk, grep, sed, coreutils (`timeout`, `od`, `sort`, `tr`, `cut`, `mktemp`), findutils and `jq`, plus `lizard` and `typos` for their rows and `claude` for `claude-validate`. `commands/v-cr/sandbox.md` S8 carries a sample Dockerfile. A tool the image lacks prints `absent: <id>: <install>`, the run reads INCOMPLETE, and the operator line reads `install: add the tool of <id> to the probe image`. A review reads INCOMPLETE until the image holds the tools of every row whose detect matches. There is no default image, and no key means the stage does not start. `cr_is_envelope_key` lists the key, so a repo file or an indication cannot set it | the container has no network, so nothing can install a tool at run time and an absent tool has to show as absent. A framework-built image would pin tool versions the operator never audited. A default image would run a name nobody chose. the kit expands empty arrays under `set -u`, which bash 4.4 accepts | `vault/decisions/ADR-009-v-cr-sandboxed-execution.md` |
| SP-2 No failure of the container path runs a repo-code probe on the host. The driver has no host-run path, and posture `sandbox` never calls `bin/probe.sh`. A container failure, a timeout and an oversize stream become `failed:` lines of that run (SP-10). A missing image, missing docker, missing `probe-image` key, missing merge base or an over-limit tree makes the driver print `probe-sandbox: <reason>` on stderr and exit 3 without starting a container. Step 3.1 then runs `--posture pr --repo "$SANDBOX_DIR"`, the clone of step 2.6, which skips every `yes` row and reads files as text; with no clone it prints `Probes: not run, no checkout`. A driver that exits 0 with failed containers is an incomplete run and has no fallback | a fallback that runs a `yes` row on the host runs pull request code exactly when the sandbox failed, which an attacker can cause by exhausting memory. `pr` is the review the operator gets without `--sandbox` | `vault/decisions/ADR-009-v-cr-sandboxed-execution.md` |
| SP-3 There are three kinds of container: the native run (`--no-repo-code`, no `--only`), one run per framework row whose `executes-repo-code` is `yes` (`--only <id>`), and one run per template row of the base (`--only <id>`, `--allow-repo-registry`). The host keeps a row only when its id is a row of that run: the ids of the framework rows with `no` for the native run, and the one `--only` id otherwise. It splits each stream by field 1, passes each id's rows through `probe_check_rows`, and on a nonzero return discards all rows of that id and writes `failed: <id>: malformed row`. Rows of any other id are dropped and the run writes `failed: <run>: rows named an id that is not a row of this run: <n>`, where `<run>` is `framework` for the native run and the id otherwise. The native run drops the kit lines `skipped: <id>: runs repo code`, because the per-row runs cover those rows. A rule run needs a `ran: <id>:` line, because its detect cell is always true, and without one the host writes `failed: <id>: no ran line`. The other runs need none, because a row whose detect is false prints nothing | the finding row has no origin, so the run and the id are the only facts the host holds. One container per tool row limits a hijacked tool to forging rows under its own id | local |
| SP-4 A base without `probes/registry.tsv` writes `no-rules: the merge base has no probes/registry.tsv` to `rules.status`, starts no rule container, and reads complete. A base row that is not the output of `rule-check.sh row` writes `skipped: <id>: hand-written row does not run in the sandbox`, whatever its stage. A base id that also names a framework probe writes `skipped: <id>: the id also names a framework probe`. A rule file that is not a regular blob (a link, or over 8192 bytes) writes `skipped: <id>: the rule file is not a regular file`. Rows past `PROBE_SANDBOX_RULES_MAX` write `skipped` for the rest. The driver writes a new one-row `registry.tsv` for each rule container, holding only the vetted template row, and never mounts the base file | a hand-written row at another stage with a template row's id would run under `--only`, and one malformed row would fail every rule container. A row that exists and did not run is a gap the critics have to know about | local |
| SP-5 The operator sees three states. Ran: the terminal `Probes:` line reads `Probes: sandbox ran` and the forge comment has no probe line. Not started: `Probes: sandbox not started, <reason>` in both, and posture `pr` ran (SP-2). Incomplete: `Probes: INCOMPLETE, <a> absent, <s> skipped, <f> failed (sandbox)` in both, and the terminal adds the `install:` lines. The reason is the text after `probe-sandbox: ` on the driver's stderr | the operator needs to tell "never ran" from "ran and lacks something", and the comment on the forge carries no install text | local |
| SP-6 `/repo` is one copy of the files `probe_files` lists, without `.git`, shared by every container of the run. The driver creates `probes/` in it and mode-sets the roots to 755 and the files to `a+rX`, because Docker cannot create a mount point in a read-only mount and uid 65534 cannot read a 0700 directory. Each rule container mounts its one-row registry and rule file at `/repo/probes`. `/in` is a directory mount that holds `changed.list`, and every mount ends in `:ro`. The container command is `/framework/bin/probe.sh diff --repo /repo --changed-list /in/changed.list`, with `--no-repo-code`, `--only <id>` or `--only <id> --allow-repo-registry` as SP-3 says. VR-4 mounted the clone and `/rules`; a clone with `.git` makes `probe_files` return 2 in a container without git, and the template row names `probes/rules/<slug>.grep` relative to the repo | the copy holds exactly the files a probe may read, and no option like `--rules-dir` is needed | local |
| SP-7 `/framework` is a copy of `bin`, `lib` and `probes` of the running framework tree made with `cp -RL`, not `git archive HEAD`. VR-4 named `git archive` | `HEAD` runs the committed code and not the code under test, and a plugin install has no `.git` | local |
| SP-8 `--base` is a full commit id of 40 or 64 hex digits. Step 2.6 records `PROBE_BASE` (moved from step 3.1) as the full id printed by `git merge-base` in the clone, after step S2 fetches the base branch with enough history for it. The driver checks that `git cat-file -t` reads `commit`, lists the base with `git ls-tree -z -l`, and reads each rule file by object id with `git cat-file blob`. It accepts a slug matching `^[a-z0-9][a-z0-9-]{0,39}$` and modes 100644 and 100755 up to 8192 bytes. A base commit missing from the clone makes the driver exit 3 with `the merge base is not in the clone` | the clone is shallow, so the base can be missing, and the clone holds attacker objects | local |
| SP-9 Posture `sandbox` tags every row `[confirmed]` and runs nothing on the host. A native row is a check of the framework on the pull request files. A tool row is a tool of the operator's image on those files, limited by SP-3 to its own id. A template row is a check written by the base, which the pull request cannot edit. `registry-edited` is not printed and nothing is demoted, because no row of the run comes from the pull request's registry. `--paths` and the row cap still apply. The reader of the four files is its own function in `bin/probe-panel.sh` | VR-5 sets these tags; the run is the only source of origin | local |
| SP-10 Each container starts as `docker run --rm --init --name <sandbox-name>-<n> --label com.vault.v-cr.sandbox=<sandbox-name> --network none --read-only --cap-drop ALL --security-opt no-new-privileges --ipc none --log-driver none --pull never --user 65534:65534 --memory <m> --memory-swap <m> --cpus <c> --pids-limit <p> --tmpfs /tmp:rw,noexec,nosuid,nodev,size=64m`, each flag and value as two words, then `-e PROBE_TOOLS_FROM=image`, `-e PROBE_TIMEOUT=<n>` and `-e PROBE_OUT_MAX=<n>`, the mounts, the image and the command. `VCR_SANDBOX_MAP` keys `memory` (default 1g), `cpus` (1) and `pids` (256) must be positive numbers with an optional unit and are clamped to 8g, 4 and 1024, and a value of 0 reads as the default. The host runs the docker client under `timeout` of `PROBE_SANDBOX_TIMEOUT` (180 seconds) and writes its two streams to files under `ulimit -f`, then cuts each at `PROBE_OUT_MAX` bytes before it scrubs. The status lines are `failed: <run>: container exit <n>`, `failed: <run>: timed out after <s> seconds` and `failed: <run>: printed more than <PROBE_OUT_MAX> bytes`. A container failure or timeout still exits 0. The host caps the copy at `PROBE_SANDBOX_FILES_MAX` (20000) files and `PROBE_SANDBOX_BYTES_MAX` (200000000) bytes, runs host git and the copy under `timeout`, copies with `cp --parents --no-dereference --no-preserve=all` from the NUL list, and stops starting containers after `PROBE_SANDBOX_TOTAL` (900) seconds, writing `skipped: <id>: the time budget is spent` for the rest. A path holding `:` or `,` is refused before it reaches `-v` | `ulimit -f` limits files and not pipes, and docker defines 0 as no limit | local |
| SP-11 The container lists files with `find`, so the tools' whole-tree scans skip `vendor/` and `node_modules/`. A rule row scans a changed file under them, because the changed list goes through `probe_filter_list` only | the alternative is git in the container, which SP-6 excludes | local |
| SP-12 `lib/probe-run.sh` is not edited: `probe_path` in `lib/probe-registry.sh` is the one place the repo tool directories join PATH. The work directory of the driver is `$(cr_sandbox_root)/<sandbox-name>-probe.XXXXXX`, a leaf of its own, so the clone at `$(cr_sandbox_root)/<sandbox-name>` is never removed by the driver | one home for the rule, and the critics still read the clone after the driver exits | local |

## Scope & non-goals
Covers the probe stage of `/v-cr --sandbox`: the driver, `--changed-list`, `PROBE_TOOLS_FROM`, the sandbox posture, the image key, the `/v-cr` text and the ADR-009 addendum. Non-goals:
- running a hand-written repo row anywhere;
- building or pulling a probe image;
- changing the C-2 row shape or a registry column;
- running probes for `/v-team` or `/v-work`;
- editing `lib/probe-scope.sh`, which the master plan lists and this design does not need.

## Artifact lifecycles

| artifact | what requires it | who writes it | who reads it | missing or wrong |
|---|---|---|---|---|
| `probe-image` key of `VCR_SANDBOX_MAP` | `bin/probe-sandbox.sh` before any container starts | the operator in the user or global environment | `cr_probe_image` in `lib/cr-sandbox.sh` | the driver exits 3 and names the key; step 3.1 runs posture `pr` |
| `framework.tsv`, `framework.status`, `rules.tsv`, `rules.status` | `bin/probe-panel.sh run --posture sandbox` | `bin/probe-sandbox.sh` after each container exits | `bin/probe-panel.sh` | a missing file reads `failed:` and the block reads INCOMPLETE |
| `--changed-list` file | `bin/probe.sh diff` in the container | `bin/probe-sandbox.sh` through `probe_changed` | `bin/probe.sh` | exit 2, and the container status reads `failed:` |
| `PROBE_TOOLS_FROM=image` | the container environment of both runs | `bin/probe-sandbox.sh` as a `-e` argument | `probe_path` in `lib/probe-registry.sh` | the repo's tool directories join PATH, so a repo `lizard` would run; `checks/sandbox-probe-SC-1.sh` fails |
| `/repo/probes` mount | the rule containers | `bin/probe-sandbox.sh` from `git cat-file` blobs | `probes/rule-grep.sh` through the row | the row reads `failed: <id>` and the run is INCOMPLETE |
| `Probes:` line and the `Probes: sandbox` states | the operator reading a review | `commands/v-cr/steps/03-review.md` | the operator and the forge comment reader | the operator cannot tell a skipped stage from a clean one |
| ADR-009 addendum | the next session that changes the sandbox | this session | `checks/sandbox-probe-SC-6.sh` | the decision is undocumented and SC-6 fails |

## Work items

| id | file (exact path) | action | tool | constraint | covers | verification | status |
|----|-------------------|--------|------|------------|--------|--------------|--------|
| W-1 | `checks/sandbox-probe-SC-1.sh` | create | Write | written before the work; exits 2 until `--changed-list` exists | SC-1 | exits 0 after W-8, W-9 | DONE |
| W-2 | `checks/sandbox-probe-SC-2.sh` | create | Write | written; envelope flags, mounts without `vault/` and `.git`, timeout, oversize, scrub | SC-2 | exits 0 after W-7 | DONE |
| W-3 | `checks/sandbox-probe-SC-3.sh` | create | Write | written; forged row, forged ran line, hand-written row script, base rules, failure modes | SC-3 | exits 0 after W-7 | DONE |
| W-4 | `checks/sandbox-probe-SC-4.sh` | create | Write | written; tags, INCOMPLETE states, usage | SC-4 | exits 0 after W-10 | DONE |
| W-5 | `checks/sandbox-probe-SC-5.sh` | create | Write | written; real docker and `vault-tests:local` | SC-5 | exits 0 after W-7, W-10 | DONE |
| W-6 | `checks/sandbox-probe-SC-6.sh` | create | Write | written; envelope key, text homes, rule count | SC-6 | exits 0 after W-11 to W-17 | DONE |
| W-7 | `bin/probe-sandbox.sh` | create | Write | subcommand `run --repo --base --sandbox-name --out`; SP-1 to SP-4, SP-6 to SP-8 and SP-10; sources `lib/cr-sandbox.sh`, `lib/probe-scope.sh` and `lib/probe-run.sh`; exit 0 ran (even with failed containers), 2 usage, 3 not started with `probe-sandbox: <reason>` on stderr; SP-3, SP-4, SP-6, SP-8 and SP-10 spell the container list, status lines, mounts, flags and limits; the work directory is `<sandbox-name>-probe.XXXXXX` under `cr_sandbox_root`, removed after `cr_sandbox_path_is_safe`, and the clone is never touched; an EXIT, INT and TERM trap removes containers by label; at most 300 lines | SC-2, SC-3, SC-5 | `checks/sandbox-probe-SC-2.sh` and `-3.sh` exit 0 | DONE |
| W-8 | `bin/probe.sh` | edit | Edit | `--changed-list <file>` (diff only, exclusive with `--base`) fills `PROBE_FILES` through `probe_filter_list` and sets `PROBE_CHANGED_FILE`; `PROBE_TOOLS_FROM` other than empty or `image` exits 2; usage text updated; at most 165 lines | SC-1 | `checks/sandbox-probe-SC-1.sh` exits 0 | DONE |
| W-9 | `lib/probe-registry.sh` | edit | Edit | `probe_path` prints plain PATH when `PROBE_TOOLS_FROM=image` | SC-1 | `checks/sandbox-probe-SC-1.sh` exits 0 | DONE |
| W-10 | `bin/probe-panel.sh` | edit | Edit | `--posture sandbox` requires `--rows-from <dir>` and refuses it beside another posture; reads the four files, tags `[confirmed]` by file, never calls `bin/probe.sh`; status from the two status files per SP-3 to SP-5; operator line `Probes: INCOMPLETE, <a> absent, <s> skipped, <f> failed (sandbox)` with `install: add the tool of <id> to the probe image`; prints `no-rules:` notes; keeps `registry-edited`, `--paths` and the caps; at most 250 lines | SC-4, SC-5 | `checks/sandbox-probe-SC-4.sh` exits 0 | DONE |
| W-11 | `lib/cr-sandbox.sh` | edit | Edit | `cr_sandbox_map_get <key>` (first match of a `;` list of `key=value`), `cr_probe_image` (name matches `^[A-Za-z0-9][A-Za-z0-9._:/@-]*$`, at most 200 characters), `probe-image` and `probe_image` in `cr_is_envelope_key`; header comment lists both functions; at most 245 lines | SC-6 | `checks/sandbox-probe-SC-6.sh` exits 0 | DONE |
| W-12 | `commands/v-cr/sandbox.md` | edit | Edit | S2 also fetches the base branch with history for `git merge-base`; new section S8, the probe stage with a sample Dockerfile: image contract (SP-1), the no-fallback sentence `The probe stage never falls back to the host` (SP-2), row checks (SP-3), base rules (SP-4, SP-8), states (SP-5), mounts (SP-6, SP-7), limits (SP-10, SP-11), `PROBE_TOOLS_FROM=image` and the sentence `An indication never sets the probe image`; teardown of S7 covers the probe containers; at most 235 lines | SC-6 | `checks/sandbox-probe-SC-6.sh` exits 0 | DONE |
| W-13 | `commands/v-cr/steps/02-gather.md` | edit | Edit | §2.6 runs `bin/probe-sandbox.sh run` after the clone exists and records `PROBE_BASE`, `PROBE_SANDBOX_OUT=<dir>` and `probe-exit=<0|3>` next to the `Sandbox:` line for step 3, with the stderr reason of exit 3; the Required output `Sandbox:` line gains `probes <ran|not started|incomplete>` | SC-6 | `checks/sandbox-probe-SC-6.sh` exits 0 | DONE |
| W-14 | `commands/v-cr/steps/03-review.md` | edit | Edit | the probe block runs `bin/probe-panel.sh run --posture sandbox --rows-from "$PROBE_SANDBOX_OUT"` when the stage ran, `--posture pr --repo "$SANDBOX_DIR"` otherwise, and `Probes: not run, no checkout` with no clone; the summary probe line and the Required output `Probes:` line carry the three states of SP-5; the file still never names `--allow-repo-registry` | SC-6 | `checks/sandbox-probe-SC-6.sh` exits 0 | DONE |
| W-15 | `commands/_shared/probe-kit.md` | edit | Edit | Commands: `--changed-list`; Trust: `PROBE_TOOLS_FROM=image` in one sentence that refers to `commands/v-cr/sandbox.md`; at most 150 lines | SC-6 | `bin/doc-lint.sh commands/_shared/probe-kit.md` exits 0 | DONE |
| W-16 | `commands/_shared/critic-panel.md` | edit | Edit | one sentence in §(a) Probe stage that names `--posture sandbox` and `commands/v-cr/sandbox.md`; no word of the rule-count patterns | SC-6 | `bin/rule-count.sh` reports at most 181 rule lines | DONE |
| W-17 | `vault/decisions/ADR-009-v-cr-sandboxed-execution.md` | edit | Edit | `## Addendum` for the probe stage: one line each for SP-1, SP-2 and SP-3 with the reason, pointing to `commands/v-cr/sandbox.md` S8 for the mechanism and never repeating the no-fallback sentence | SC-6 | `bin/doc-lint.sh` exits 0 | DONE |
| W-18 | `tests/fixtures/sandbox-probe/fake-docker.sh` | create | Write | test double of docker for the graders and `sandbox-probe.bats` | SC-2, SC-3 | `checks/sandbox-probe-SC-2.sh` exits 0 | DONE |
| W-19 | `tests/fixtures/sandbox-probe/mk-repo.sh` | create | Write | fixture repo builder | SC-2 to SC-5 | `checks/sandbox-probe-SC-2.sh` exits 0 | DONE |
| W-20 | `tests/unit/sandbox-probe.bats` | create | Write | Test backlog rows T-3 to T-9 and T-12 to T-14 | SC-1 to SC-7 | `./tests/run.sh tests/unit/sandbox-probe.bats` | DONE |
| W-21 | `tests/unit/cr-sandbox.bats` | edit | Edit | Test backlog rows T-1 and T-2 | SC-6 | `./tests/run.sh tests/unit/cr-sandbox.bats` | DONE |
| W-22 | `tests/unit/v-cr.bats` | edit | Edit | Test backlog row T-10; guard T-11 keeps its three assertions | SC-6 | `./tests/run.sh tests/unit/v-cr.bats` | DONE |
| W-23 | `checks/sandbox-probe-SC-7.sh` | create | Write | written; the touched suites and the full suite | SC-7 | exits 0 after W-20 to W-22 | DONE |
| W-24 | `vault/plans/2026-09-21-0900-architecture-first-planning.md` | edit | Edit | row S10 `done` with evidence, its file list matches this plan, C-8 states the `[confirmed]` tag for both files | SC-6 | `bin/doc-lint.sh` exits 0 | DONE |
| W-25 | `vault/plans/2026-09-21-1600-sandbox-probe.arch.md` | create | Write | structure spec of this plan | | `bin/gate.sh arch` prints `arch: ok` | DONE |
| W-26 | `vault/plans/2026-09-21-1430-v-rule.md` | edit | Edit | outside the S10 file list: row W-16 named the graders of this plan, and `bin/gate.sh criteria` refuses a check that two plans claim; the row now says `five graders` | SC-6 | `bin/gate.sh criteria vault/plans/2026-09-21-1600-sandbox-probe.md` exits 0 | DONE |
| W-27 | `checks/rule-SC-7.sh` | edit | Edit | outside the S10 file list: its master-plan test requires the S10 row to read `todo`, which fails once this session marks it `done`; the test accepts `todo` or `done` | SC-6 | `checks/rule-SC-7.sh` exits 0 after W-24 | DONE |

## Sequencing & dependencies
Order: graders and fixtures (done), W-9, W-8, W-11, W-7, W-10, W-12 to W-17, W-20 to W-22, W-24, W-27. S10 needs S8 (done). One commit that stages this plan's files and leaves `output-styles/director.md` and `scripts/completion-hook.sh` unstaged.

## Rollback
Revert the session commit. Nothing calls `bin/probe-sandbox.sh` until step 3.1 and step 2.6 run it, and they run it only under `--sandbox`. A review without `--sandbox` is unchanged.

## Test plan
`tests/unit/sandbox-probe.bats` runs each grader with its message on failure, and guards the text homes. `tests/unit/cr-sandbox.bats` covers the pure helpers. Failure modes covered by the graders:
- a container that prints a forged row or ran line;
- a base row that runs a script printing a forged template row;
- a hung container and an oversize stream;
- a missing image, missing docker and missing key;
- `/framework` holding `vault/` or `.git`;
- a repo tool directory on PATH.

## Test design dossier
Not generated. The planner wrote the test design from the criteria and the failure modes above.

## Test backlog

| id | source | kind | target (exact path) | intent | priority | disposition |
|----|--------|------|---------------------|--------|----------|-------------|
| T-1 | SP-1 | unit | `tests/unit/cr-sandbox.bats` | `cr_probe_image` prints the first `probe-image`, and returns 1 for an unset map, a missing key, and the names `-x`, `a b`, `a$(id)` and `../x` | must | implement |
| T-2 | SP-1 | unit | `tests/unit/cr-sandbox.bats` | `cr_is_envelope_key` returns 0 for `probe-image` and `probe_image`; `cr_sandbox_map_get` prints a value with `=` inside it | must | implement |
| T-3 | SP-6 | unit | `tests/unit/sandbox-probe.bats` | the envelope and mount grader passes | must | implement |
| T-4 | SP-2 | unit | `tests/unit/sandbox-probe.bats` | the trust grader passes: forged row, hand-written script, no host run | must | implement |
| T-5 | SP-9 | unit | `tests/unit/sandbox-probe.bats` | the panel grader passes | must | implement |
| T-6 | SP-12 | unit | `tests/unit/sandbox-probe.bats` | the `probe.sh` grader passes | must | implement |
| T-7 | SP-2 | unit | `tests/unit/sandbox-probe.bats` | `bin/probe-panel.sh` never names `bin/probe.sh diff` inside its sandbox branch and `bin/probe-sandbox.sh` never names `bin/probe.sh` outside the container command | must | implement |
| T-8 | SP-2 | unit | `tests/unit/sandbox-probe.bats` | `commands/v-cr/sandbox.md` holds the no-fallback sentence once and no other command file repeats it | should | implement |
| T-9 | SP-5 | unit | `tests/unit/sandbox-probe.bats` | the delivery grader is skipped with a message when docker is unusable, and never marks the case ok | should | implement |
| T-10 | SP-5 | unit | `tests/unit/v-cr.bats` | `03-review.md` names `--posture sandbox --rows-from` and the three `Probes: sandbox` states, and `02-gather.md` names `bin/probe-sandbox.sh` | must | implement |
| T-11 | SP-2 | unit | `tests/unit/v-cr.bats` | the existing guard T-11 still passes: posture `pr` on a checkout, `no checkout`, no `--allow-repo-registry` | must | implement |
| T-12 | SP-3 | unit | `tests/unit/sandbox-probe.bats` | three status lines of one container reach `framework.status` as three lines | must | implement |
| T-13 | SP-10 | unit | `tests/unit/sandbox-probe.bats` | files named `-rf` and `--help` cannot hide the tree size from `PROBE_SANDBOX_BYTES_MAX` | must | implement |
| T-14 | SP-4 | unit | `tests/unit/sandbox-probe.bats` | the last base row without a trailing newline runs, and a rule named `all` keeps the framework-id guard | should | implement |

## Refs
- `vault/plans/2026-09-21-0900-architecture-first-planning.md`: the master plan; this is session S10 and contracts C-2, C-3, C-6 and C-8.
- `vault/plans/2026-09-21-1430-v-rule.md`: decisions VR-2, VR-4 and VR-5, the design this plan builds and revises.
- `commands/v-cr/sandbox.md`: the S0 envelope the containers use.
- `commands/_shared/probe-kit.md`: the kit contract, the rule files and the trust rules.
- `vault/decisions/ADR-009-v-cr-sandboxed-execution.md`: no pull request code runs outside the envelope.
- `vault/decisions/ADR-003-tool-grounded-findings.md`: a finding is confirmed only when a tool run produced it.
