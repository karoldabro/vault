---
type: index
project: vault
tags: [index]
---

# vault — Feature index

| Feature | Status | Last touched | Notes |
|---------|--------|--------------|-------|
| [[features/lifecycle-hooks]] | in_progress | 2026-06-22 | Per-project VAULT.md `hooks` (14 instruction-only phases) + `tools` (task-tracker MCP) + step-1 `/rename` suggestion; shared by /v-work + /v-team (ADR-010) |
| [[features/v-team]] | in_progress | 2026-09-13 | Persona-critique lifecycle; api pack full, nuxt/flutter draft; testing critic group + generative test-design fan-out (f2) + system-domain-expert (ADR-011). **+ business persona family** (sales/seo/support/business/startup-eval, shared business/data-evidence, multi-pack seating + §2.2 selection, ADR-016). **+ evidence-based panel hardening** (critic-owned grounding, minority flag, verifier asymmetry, pre-mortem technique; living catalog `research/llm-collaboration-patterns`, ADR-017). **+ guaranteed consumer seat** (simulates the receiver; `templates/plan.md` `## Artifact lifecycles`; doc-lint PLAN1/PLAN2). **+ Step 4 kill criterion** (`bin/gate.sh coverage`; a criterion no work item reaches sets `status: failed` and opens no approval gate) |
| [[features/v-cr]] | in_progress | 2026-09-09 | Automated remote-PR code review; GitHub+Bitbucket forges, Jira+Asana tasks; single-pass panel. Optional --sandbox path (ADR-009): runtime-verified review in a throwaway clone+Docker sandbox. Panel spawn mandatory + proven (`Spawned:`); coverage computed from per-file critic receipts via `cr_coverage`, never asserted; delivery read back from the forge; indications routed to the changed files and every citation anchor-verified, with the unreachable rules printed |
| [[features/v-pm]] | in_progress | 2026-07-03 | Cross-project feature planning: business→product→architect→contract pipeline drafts a project-agnostic plan + structured contracts into a shared `_features/` vault; per-project `/v-team` sessions coordinate via file-based conversation threads (ADR-013). **+ business-logic `requirements.md` knowledge center** (rules REQ-NN + axis tags, glossary, decision/state tables) for 1+ repos; single-repo `requirements/` category; id chain → established `features/` dossier at shared /v-capture §5b (ADR-014) |
| [[research/decision-communication]] | shipped | 2026-08-03 | **User-facing communication contract** — one shared `_shared/communication.md` bound in 12 dispatchers + 15 Required-output step files; two-layer output (decision to terminal, design to artifact) as a net deletion; mandatory `Impact` at approval gates; omit-when-empty scoped to green states only; ask-gate before question shape; synthesizer caps subagent text (output styles can't reach subagents); opt-in `output-styles/director.md` for all sessions (ADR-018). Evidence: `research/decision-communication` |
| [[features/install-distribution]] | in_progress | 2026-09-15 | Two mutually exclusive install routes: Claude Code plugin (repo is its own marketplace, pinned `version`) and the `install.sh` symlink clone; `$VAULT_FRAMEWORK_PATH` resolves `${CLAUDE_PLUGIN_ROOT}` first and is never persisted; `/v-setup` is the consent-gated dependency step; SessionStart hook detects without installing (ADR-020); three install profiles — `--light` default, `--full` adds the developer tools Serena + Graphify, recorded as `install_mode` so a light machine is never reported as broken (ADR-021) |
| [[features/vault-git-sync]] | shipped | 2026-08-04 | Out-of-repo vaults (`~/vault/<slug>/`, `~/vault/_features/`) pull before the lifecycle reads and commit+push after it writes, through `bin/vault-sync.sh`; exit code is the interface (0/1/3/4/5) and none is fatal — a sync failure never halts a lifecycle or blocks a capture; raw `git`, `git add -A`, `git init` and remote-branch creation are all banned; `/v-ask` excluded (it promises no git write); toggle `behaviour.vault_autosync`, default on (ADR-022) |
| [[features/v-method]] | shipped | 2026-09-13 | Writes the method for one heavy task: ordered stages, each carrying its command, seats, tools, exit evidence and a kill criterion naming the exact file and field its verdict is read from. Fourteen routing rows keyed on checkable task properties, never a complexity classification; the routing is recorded as a heuristic with every answer kept, because no validated instrument maps a task to a process and no study shows a methodology written in advance improves the outcome. Intake asks which of budget and criteria is fixed, since the two are exclusive. Writes the method and runs no stage (ADR-029) |
| [[features/plugin-extensions]] | in_progress | 2026-09-14 | Two extension points so a separate repo extends the framework: `init` scaffolds a repo's files during `bin/vault-init.sh`, `dod-keys` makes `bin/gate.sh config` require a plugin's `VAULT.md` keys — only where the repo's flat `plugins:` scalar names it. Four were designed; `setup` had no consumer and `install` could never fire, since `setup.sh` skips `install.sh` in plugin-install mode. Commands and hooks come from the plugin's own Claude Code manifest, which already composes across 23 installed plugins. Compatibility is a capability probe over a version compare. `bin/vault-plugin.sh` is the only writer of `~/vault/_global/plugins.tsv`, stores the realpath and refuses a symlinked path: registering a plugin trusts every future commit of that repository. A point runs as a child process through its own shebang with stdin closed; exit 1 declined, above 1 could not run (ADR-030). First consumer: `github.com/karoldabro/vault-quality-gates`, designed and unbuilt |
| [[features/session-continuity]] | in_progress | 2026-09-14 | Two commands carry work across a session boundary. `/v-handoff` writes what the next session continues into `handoffs/` — eight core sections always at the top (left to do, do not touch, unverified, goal, task, requirements, next command, blockers), six more only when the session has them, because the shift-handover failure is the critical line buried under routine. `resume` picks the newest `status: open` file, follows its `continues` chain backwards, and marks it resumed; it refuses when nothing is left to do and names `/v-capture` instead. `/v-report` writes a problem found during other work into `reports/`, with the finder in frontmatter because document-standard rule 7 bars an author's name in a contract body. Both folders have a reader: `02-load-context.md` §2.6a lists what is open before the first edit. A problem inside the current plan's scope stays in that plan's `## Open & deferred`; `vault/defect-ledger.md` stays the post-repair recurrence record |
