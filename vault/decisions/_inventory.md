---
type: index
project: vault
tags: [index, decisions]
---

# vault — Decisions inventory

| ID | Title | Date | Status |
|----|-------|------|--------|
| [[ADR-001-panel-loop-over-peer-debate]] | Panel→synthesize→re-loop over peer debate | 2026-06-16 | accepted |
| [[ADR-002-no-stop-on-approval-alone]] | Critique loops never converge on approval alone | 2026-06-16 | accepted |
| [[ADR-003-tool-grounded-findings]] | Persona findings are tool-grounded (confirmed vs advisory) | 2026-06-16 | accepted |
| [[ADR-004-generic-packs-specifics-in-indications]] | Generic packs; project specifics in indications | 2026-06-16 | accepted |
| [[ADR-005-installer-auto-exec]] | setup.sh auto-installs the tool stack on Ubuntu (consent-gated) | 2026-06-18 | accepted |
| [[ADR-006-testing-critic-group]] | Testing critique is a one-cluster-per-persona grounded group | 2026-06-19 | accepted |
| [[ADR-007-light-siblings-guardrail]] | Light command siblings drop the approval gate for a scope guardrail | 2026-06-19 | accepted |
| [[ADR-008-v-cr-remote-pr-review]] | /v-cr reviews remote PRs by reusing the panel single-pass, precision-first, untrusted-input | 2026-06-19 | accepted |
| [[ADR-009-v-cr-sandboxed-execution]] | /v-cr optional --sandbox path: runtime-verified review, framework-owned isolation envelope, attribution-aware test gate | 2026-06-19 | accepted |
| [[ADR-010-lifecycle-hooks-tools-rename]] | Per-project lifecycle customization via instruction-only VAULT.md hooks + tools; rename is a suggestion | 2026-06-22 | accepted |
| [[ADR-011-generative-test-design-subphase]] | Test design is a generative PROPOSE sub-phase; generators emit pre-impl, critics confirm post-impl | 2026-06-29 | accepted |
| [[ADR-012-propose-clarify-research-gates]] | PROPOSE opens with clarify + online-research front gates (shared §3a); contradicting consensus reconciled in writing | 2026-07-03 | accepted |
| [[ADR-013-v-pm-cross-project-planning]] | /v-pm plans cross-project features into a shared `_features/` blackboard workspace; file-based conversation (state-in-filename), derived ledger, auto-pickup + `/v-pm status` push, deterministic contracts-drift | 2026-07-03 | accepted |
| [[ADR-014-vpm-business-knowledge-center]] | /v-pm authors a `requirements.md` business-knowledge spec (rules REQ-NN, glossary, decision/state tables) decoupled from the coordination machinery (1+ repos; single-repo → `requirements/` category); id chain spec→backlog→established dossier; no cross-repo write | 2026-07-03 | accepted |
| [[ADR-015-retier-lifecycle-lite-critic-fast-path]] | Re-tier lifecycle: lite critic in /v-work, auto fast path, /v-team framed as escalation (usage-data driven) | 2026-07-04 | accepted |
| [[ADR-016-business-persona-family]] | Business persona family: opt-in packs (sales·seo·support·business·startup-eval), shared `business/data-evidence` numeric critic, multi-pack seating (one architect seat, §2.2 priority order, cross-pack suppression) | 2026-07-10 | accepted |
| [[ADR-017-evidence-based-panel-hardening]] | Evidence-based panel hardening: verifier tool-asymmetry corollary, pre-mortem as skeptic technique (seat rejected — correlated double-vote), minority-flag dissent surface + critic-owned grounding; catalog `research/llm-collaboration-patterns` is the living evidence reference | 2026-07-10 | accepted |
| [[ADR-018-decision-communication-contract]] | One shared `_shared/communication.md` governs all user-facing prose (bound by installed path in 12 dispatchers + 15 Required-output step files); two-layer output as a net deletion; mandatory `Impact` at gates; omit-when-empty scoped to green only; ask-gate before question shape; synthesizer caps subagent text; opt-in `director` output style; /v-cr forge rule kept (different reader); evidence in `research/decision-communication` | 2026-08-03 | accepted |
| [[ADR-020-claude-code-plugin-distribution]] | Ship the framework as a Claude Code plugin alongside the `install.sh` symlink route (mutually exclusive, enforced by both installers); repo is its own single-plugin marketplace (`source: "./"`); `version` pinned so publishing is a deliberate bump; `$VAULT_FRAMEWORK_PATH` resolves `${CLAUDE_PLUGIN_ROOT}` first and is never persisted; `commands/` holds commands only; `/v-setup` is the consent-gated dependency step and the SessionStart hook detects without installing (ADR-005 line holds) | 2026-08-04 | accepted |
| [[ADR-021-install-profiles]] | Serena is the one developer tool: two named profiles (`--light` default, no optional tools / `--full` = Serena), interactive prompt when no flag is passed, `[ -t 0 ]`-gated so a piped run falls to light rather than hanging; profile recorded as `install_mode` in `~/vault/_global/config.md`, anything but `full` read as no developer tools; "a light machine is not a broken machine" — detect-stack never names Serena, doctor labels it `(developer)`, tool-playbook + v-work §2.4/§2.5 + v-do read `install_mode` before offering an install | 2026-08-04 | accepted |
| [[ADR-022-vault-git-autosync]] | Out-of-repo vaults sync automatically through `bin/vault-sync.sh` (pull before reading, commit+push after writing), bound by `_shared/vault-sync.md`; exit codes are the interface (0/1/3/4/5) and none is fatal; raw `git` against a vault is banned, as are `git add -A`, `git init`, and remote-branch creation; `/v-ask` excluded because it promises no git write; toggle `behaviour.vault_autosync` (default on) | 2026-08-04 | accepted |
| [[ADR-023-document-writing-standard]] | A second shared module `_shared/document-standard.md` governs every file a command writes, bound in 14 step files; two axes because filing rules alone let four of five defect examples through — register rules (conclusion before evidence, say it not allude, name the actor, headings in words the reader has) join them; three classes (contract/record/message) and the process record moves to a `plans/<slug>.trail.md` sidecar, verified safe because nothing reads a critique trail back; enforcement is `bin/doc-lint.sh` + a non-blocking `PostToolUse` hook, because prose alone already failed; `--compare` gates every shortening; `/v-reconcile` applies it to documents that already exist | 2026-08-21 | accepted |
| [[ADR-025-mechanical-brevity-enforcement]] | The brevity rules already load twice per session, through `~/.claude/CLAUDE.md` and the director output style, and replies stayed long — so measure instead of restating. `bin/output-lint.sh` measures one reply (lines, words, sentences over 25 words, banned filler from `lib/doc-lint-patterns.tsv`); `scripts/output-lint-hook.sh` on `Stop` records it per session; `scripts/brevity-reminder-hook.sh` on `UserPromptSubmit` names, at the next turn, only the limits that reply broke, and prints nothing when it broke none. Three triggers, each a written rule: a sentence over 25 words, banned filler, and over 15 lines in a decision block, recognised by two of its six field labels; the shouty codes match case-sensitively so "the major risk" does not trip them. Rejected: exit 2 from `Stop` (it continues the conversation, so the reply grows), printing the target on every turn (a limit shown after a compliant reply becomes a figure to fill), and generating the output style from the contract (the two deliberately differ, so a generator would carry the divergence). Both prose files gain a worked before/after table. Inert until `install.sh --enable-brevity`; `BREVITY=off` silences them. Uninstalls itself on 2026-09-10 if the median does not move | 2026-09-03 | accepted |
| [[ADR-024-vpm-pm-discipline]] | `/v-pm` gains four things a working PM has: `_shared/elicitation.md` (technique menu, a checkable stopping rule, unanswered questions become stated defaults rather than a block — a Definition of Ready stalls work); `_shared/definition-of-done.md` in two tiers, every line `met`/`failed`/`not-applicable **with a reason**`, gated at `05-commit-capture` §5.0 **before** staging because after the commit it blocks nothing; `/v-pm` emits an `## Appetite` + `## First slice` and never a task list, each repo's own `/v-team` decomposing at `(f3)`; the tracker lives in the shard the working session already opens, rows carry `evidence` + `last touched`, and feature status is **derived** by `/v-capture` Step 4e. Amends ADR-012's research gate for `/v-pm` only. The "9 of 12 features never executed" figure that first motivated this was reading a stale `header.md`; the true figure is 3 of 12, and that measurement error was the tracking defect itself | 2026-09-01 | accepted |
| [[ADR-026-mechanical-session-gates]] | A check that runs replaces a rule that is written. `bin/gate.sh` refuses on nine conditions; a criterion names a **committed script**, never a command typed into the plan, and `verdict --run` executes it and writes the verdict and captured output itself — the two cells a session never authors. Every change carries one `kind: delivery` criterion: run the real system, find this change in what the run produced, reading the artifact rather than the JSON the system wrote about itself. `scripts/completion-hook.sh` blocks a turn that marked work done with no verdict. Rejected: an agent verifying the work (no judge configuration exceeds 0.65 AUROC, 0.54 on traces, while an independent process reading state cuts false success from 44–52% to 3%), a gate before the model writes prose (no hook fires there), and graph reachability (the framework ships no code graph; `bin/gate.sh readers` greps). A rule is written so the model can decide it from the text it is writing (92.0% versus 64.3% for rules needing a count), and one needing a count gets a check or is deleted; grammar is not the lever — measured here, prohibitions score 89.5% and requirements 76.9%. Compliance still falls as instruction count rises, and the best model satisfies every constraint of a multi-part instruction under 30% of the time. A check wrong more than one in ten is deleted. The instruction cut is keyed on `research/rule-compliance.md`, which dropped the prohibition rewrite and narrowed the deletion | 2026-09-04 | accepted |
| [[ADR-027-autonomous-test-fix-loop]] | The precondition and the command's scope are now set by [[ADR-030-task-agnostic-campaign-engine]]; a soft precondition is how `/v-team` reached 78% of runs, and `/v-loop` still sits outside the `/v-do` → `/v-work` → `/v-team` ladder. Grounded in two campaigns on different stacks (Laravel/Playwright, Flutter on a phone) that converged on nine rules independently; the conflict-scope taxonomy and browser rules stayed adapter-local when a third campaign used neither, and now live in `commands/v-loop/adapters/test-and-repair.md`. Round cap and per-defect retry cap both default 3, with `rounds_used` in `STATE.md` so a usage limit does not reset the cap; the fixer never produces its own retest verdict where the verifier is a model. Concurrency is **disjoint conflict sets**, not a count — the only rule that needed a counter, so no `bin/campaign.sh` ships. `campaign-rules.md` restates no rule any of the seven `commands/_shared/` modules owns (16 of the source rules already had a home), enforced by `checks/v-loop-SC-3.sh`. Case ledger is append-only JSONL; `results/` is gitignored. `scripts/staging-hook.sh` extended from `git add -A` to `git commit -a`/`-am`/pathspec-less/`--amend`/`git reset --hard` and registered, finishing D-006. Open contradiction: `bin/rule-count.sh` cites prohibitions decaying 73%→33% by turn 16, this repo measured 89.5% against 76.9% the other way, and a campaign is the first thing here that reaches turn 16 routinely | 2026-09-06 | accepted |
| [[ADR-030-task-agnostic-campaign-engine]] | `/v-loop` is a loop engine and the task is an adapter: `commands/v-loop.md` holds enumerate → work → verify → repair → re-verify → stop, and `commands/v-loop/adapters/<name>.md` answers what a case is, who works it and what decides it. **Two adapters are the floor** — one is indistinguishable from no adapter layer — and `checks/v-loop-adapter-slots.sh` refuses below two, refuses a missing slot, and refuses two adapters that verify the same way. The precondition becomes **two facts, never a judgement**: the operator names the arena in words, and names the restore command, which the session runs at intake and proves by re-reading the arena, because `git checkout -- <path>` leaves every untracked file in place. The **discriminator belongs to the adapter**: a test carries a contrary condition on the case row, a document rewrite cannot, so `document-corpus` uses control rows that must come back unchanged. **Verification is conditional on the verifier** — a second agent only where the verifier is a model, since a command's exit code cannot be flattered. The **definition of done is the verifier plus any clause citing a written rule**, which also binds enumeration, because a backlog built from the verifier alone answers how much the tool can see rather than how much work there is. A **brief carries commands, never results**: three briefs in the proving campaign carried a wrong number and each was caught only because the agent went to the source. A conflict scope names **every path** the unit writes, and a verdict expires when its case changes. Grounded in a third campaign on a task with no tests, no browser and no running application: 21 cases, 17 documents, 100 violations, 0 failures, both controls byte-identical (`vault/campaigns/2026-09-15-0933-doc-corpus/`). Watch: whether `/v-loop` is reached for where `/v-do` would do, now that its entry condition is wider | 2026-09-15 | accepted |
| [[ADR-029-methodology-command]] | `/v-method` writes a method file and runs no stage — each stage names an existing command with its own gates, because a command that both chose the method and executed it would grade its own choice. One command file plus one reference file, not a dispatcher and a steps directory. Fourteen routing rows key on checkable task properties, each carrying its method, artifact and stopping rule; a complexity-domain classifier and a predictability two-by-two are both rejected, the latter repudiated by its own author for this use. The routing is recorded as a heuristic and every answer is kept, the ones that did not fire included, because that record is the only route to evidence this command will have — no study measures whether writing a methodology in advance improves the outcome, and only its parts carry evidence. A task firing no property gets the fixed localise-repair-validate pipeline, the baseline an agent architecture has to beat. Intake asks which of budget and criteria is fixed, since the two are exclusive, and the method file records what the other lost. Every stage row carries command, seats, tools, exit evidence and a kill criterion naming the exact file and field its verdict is read from; criteria are written before the stage and `bin/gate.sh verdict --run` produces each verdict. A seat enters only when it owns an affordance one worker lacks, three by default and five the ceiling. `/v-pm` owns what the product must do and `/v-method` how to work on it, consuming the appetite without re-deriving it. The owned-rule list moved to `lib/shared-module-rules.tsv` so two duplication checks share it. Four claim-audit signals ship nothing and the reference says so; no check reads a written method file yet, so the per-stage field rules are prose | 2026-09-13 | accepted |
| [[ADR-030-framework-extension-points]] | Two extension points, `init` and `dod-keys`, so a separate repo can scaffold per-repo files during `bin/vault-init.sh` and declare the `VAULT.md` keys `bin/gate.sh config` then requires. Four were designed; evidence cut two. `setup.sh:423-427` skips `install.sh` whenever the framework is plugin-installed, so an `install` point could never fire on this machine, and `install.sh:169-175` only symlinks a hook while registration at `:207-232` needs the event and matcher — a point handed the hooks directory ships a hook Claude Code never calls. 23 plugins already compose their own commands and hooks, so a `commands` point would duplicate a working mechanism with a broken one; the one consumer had nothing machine-level for `setup` either. A plugin's keys bind only where the repo names it in a flat `plugins:` scalar, because otherwise registering one plugin refuses every repo on the machine; a second `plugins:` line is refused, since the reader takes the first and would drop the rest in silence. Compatibility is a capability probe over a version compare — the version string moved in 7 of 173 commits and was stale on `main` — while Claude Code's own `dependencies` key gates install time. `bin/vault-plugin.sh` is the only writer of `~/vault/_global/plugins.tsv`, stores the `realpath`, refuses a path traversing a symlink, and scans for nothing: registering a plugin trusts every future commit of that repository, which is the one place this framework contradicts `commands/v-cr/sandbox.md`. A point runs as a child process through its own shebang with stdin closed, never sourced and never forced through `sh`; exit 1 declined, above 1 could not run. One consumer justifies the whole API | 2026-09-14 | accepted |
| [[ADR-031-architecture-first-planning]] | A plan is four files: agent plan, `plans/<slug>.arch.md` structure spec, trail, and a generated human Artifact linked from `human_plan`. A profile is a data-file pair `arch-profiles/<name>.tsv` and `.md`, loaded by name from the repo first and then the framework, so a new project type needs no code change; a new `arch_profile` key names it (or `none`; absent means `none`) because `dod_profile` is `code` in every operator repo, this harness repo included. `bin/gate.sh arch` checks the spec against `commands/_shared/architecture-spec.md` at PROPOSE finalise and again at the `/v-team` approval gate, so omitting the spec cannot skip it. A script renders the human page from the plan and spec, and a gate compares it byte for byte with a fresh render, so a status flip never makes it stale; the probe kit lives in this framework, not `vault-quality-gates`, which gates pushes against a baseline. A spec is type `arch-spec`, cap 300 lines. Every master plan carries a `## Cross-session contracts` table, gated later, so a consuming session never redefines a shape. Motivated by 97.0% of structural failures evading type checking and 100% evading tests and security scans (`vault/research/ai-code-slop.md`). Longer planning has no controlled evidence, so its added cost is capped at 50% of PROPOSE | 2026-09-21 | accepted |
| [[ADR-032-remove-memory-and-edit-mcps]] | The framework prescribes no memory plugin, edit MCP or code graph; Serena is the one optional developer tool (`setup.sh --full`). Profiles are `--light` (no tools, default) and `--full`; removed flags exit 2. Recall is grep over the vault; project-wired MCPs stay. Users remove old tools with `docs/uninstall-removed-tools.md` | 2026-09-24 | accepted |
