---
type: feature-workspace
slug: {{feature}}
participants: [{{participants}}]
status: planning   # planning | in-progress | shipped
created: {{date}}
session_opens: {}   # {proj: n} — bumped by each project's Step 0 pickup; used to age threads
---

# {{feature}} — cross-project feature workspace

Project-agnostic plan + contract + conversation for a feature spanning {{participants}}. Created by
`/v-pm`. Source of truth: `generic-plan.md` (only `/v-pm` writes) + `contracts.md`. Coordinate via
`conversation/` threads; see what's waiting any time with `/v-pm status`.
