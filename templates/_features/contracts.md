# {{feature}} — contracts (the cross-project seam)

**Structured on purpose** — the per-project `/v-team` pickup diffs against this mechanically to catch
drift, so keep it as data (tables / typed blocks), not prose. Single source of truth for the
api↔frontend interface. Only `/v-pm` (plan / reconcile) writes it.

## Endpoints
| method | path | request | response | notes |
|--------|------|---------|----------|-------|
|        |      |         |          |       |

## Enums
| name | values | notes |
|------|--------|-------|
|      |        |       |

## Data shapes
<!-- Typed field lists per entity — `field: type · required? · notes`. -->

## Events / queues
| name | payload | producer | consumer |
|------|---------|----------|----------|
|      |         |          |          |
